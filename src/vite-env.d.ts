/// <reference types="vite/client" />

// Brings in Vite's ambient module declarations — `import beeUrl from './x.svg'`
// resolving to a string URL, and the types for `import.meta.env` (BASE_URL and
// PROD, both of which main.tsx reads to register the service worker).
